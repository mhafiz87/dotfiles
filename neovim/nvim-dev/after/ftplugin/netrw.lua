-- noma: when off, not modifiable
-- nomod: when on, not modifiable
-- nu: show line numbers
-- rnu: show relative line numbers
-- nobl: don't include in buffer list
-- nowrap: don't wrapt
-- ro: read only
vim.cmd([[let g:netrw_bufsettings = "noma nomod nu rnu nobl nowrap ro"]])
